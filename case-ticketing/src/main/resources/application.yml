spring:
  profiles:
    active: local
  application:
    name: case-ticketing

server:
  port: 8400
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  metrics:
    tags:
      application: ${spring.application.name}
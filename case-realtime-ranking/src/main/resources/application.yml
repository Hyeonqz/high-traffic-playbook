spring:
  profiles:
    active: local
  application:
    name: case-realtime-ranking

server:
  port: 8300
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  metrics:
    tags:
      application: ${spring.application.name}